import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class sixth extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sixth Page'),
      ),
      body: Text('This is sixth page.',),
    );
  }
}
